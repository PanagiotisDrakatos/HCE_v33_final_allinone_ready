# HCE Backtest Engine Environment Variables
# Copy this file to .env and fill in your values
# Usage: cp .env.example .env

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================

# Python logging level for the application
# Controls verbosity of logs during backtest execution
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
HCE_LOG_LEVEL=INFO

# ==============================================================================
# INTEGRATION TEST SETTINGS
# ==============================================================================
# These variables are only used when running integration tests.
# They are NOT needed for normal application usage.

# Enable ClickHouse integration tests
# Set to "1" to enable, "0" or unset to disable
# Default: 0 (disabled)
IT_CLICKHOUSE=0

# Enable TimescaleDB integration tests
# Set to "1" to enable, "0" or unset to disable
# Default: 0 (disabled)
IT_TIMESCALE=0

# ClickHouse server URL for integration tests
# Only used when IT_CLICKHOUSE=1
# Default: http://localhost:8123
CLICKHOUSE_URL=http://localhost:8123

# TimescaleDB connection string for integration tests
# Only used when IT_TIMESCALE=1
# Format: postgresql://user:password@host:port/database
# Default: postgresql://postgres:postgres@localhost:5432/hce
TIMESCALE_DSN=postgresql://postgres:postgres@localhost:5432/hce

# ==============================================================================
# CI/CD SETTINGS (GitHub Actions)
# ==============================================================================
# These are automatically provided by GitHub Actions and should NOT be set
# manually in local .env files. They are documented here for reference only.

# GitHub API token (auto-provided by GitHub Actions)
# Used for: PR comments, workflow interactions
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxx

# CodeCov upload token (GitHub Secret)
# Used for: uploading coverage reports to codecov.io
# Optional - only needed for main branch coverage upload
# CODECOV_TOKEN=xxxxxxxxxxxxxxxx

# ReviewDog GitHub API token (GitHub Secret)
# Used for: inline PR review comments via reviewdog
# Falls back to GITHUB_TOKEN if not set
# REVIEWDOG_GITHUB_API_TOKEN=ghp_xxxxxxxxxxxxx

# ==============================================================================
# DOCKER SETTINGS
# ==============================================================================
# Docker Compose services use settings defined in docker-compose.yml
# Database connection settings are configured there, not via environment variables.
# See docker-compose.yml for:
#   - ClickHouse: CLICKHOUSE_DB, CLICKHOUSE_USER, CLICKHOUSE_PASSWORD
#   - TimescaleDB: POSTGRES_PASSWORD, POSTGRES_USER, POSTGRES_DB

# ==============================================================================
# NOTES
# ==============================================================================

# 1. Application Configuration (NOT Environment Variables)
#    Most application settings are configured via YAML config files, not env vars.
#    See examples/cfg.yaml for configuration options:
#      - Database URLs (clickhouse_url, timescale_dsn)
#      - Batch processing settings
#      - Fill model parameters
#      - Run identification

# 2. Local Development
#    For local development, you typically only need:
#      - HCE_LOG_LEVEL (optional, defaults to INFO)
#    Integration tests also require:
#      - IT_CLICKHOUSE and/or IT_TIMESCALE
#      - Corresponding database URLs if different from defaults

# 3. Running Integration Tests
#    Start databases with Docker:
#      docker-compose up -d
#    Then enable tests:
#      export IT_CLICKHOUSE=1
#      export IT_TIMESCALE=1
#      pytest -q -m "integration"

# 4. CI/CD Secrets
#    CI/CD environment variables are configured as GitHub Secrets.
#    See .act.secrets.example for local GitHub Actions testing with 'act'.
