[pytest]
addopts = -q -m "not integration" --cov=hcebt --cov-report=xml --cov-report=term-missing --cov-fail-under=80 --maxfail=1
markers =
    integration: marks tests that require external services (deselect with '-m "not integration"')
testpaths = tests
