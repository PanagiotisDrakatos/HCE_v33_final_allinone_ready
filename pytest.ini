[pytest]
addopts = -q -m "not integration" --cov=hcebt --cov-report=xml --cov-report=term-missing --maxfail=1
markers =
    integration: marks tests that require databases (deselect with '-m "not integration"')
