[pytest]
addopts = -q --maxfail=1 --cov=hcebt --cov=lib --cov-report=term-missing:skip-covered --cov-report=xml --cov-fail-under=85
markers =
    integration: marks tests that require external services (deselect with '-m "not integration"')
testpaths = tests
